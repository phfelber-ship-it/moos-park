export const metadata = {
  robots: { index: false, follow: false },
  title: "Teilnahme absagen – THE EVENT EXPERIENCE | moos.park",
};

export default function AbsagenLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
